# Copy to .env and adjust. The MCP server refuses to start without a token.
MCP_BEARER_TOKEN=changeme
# Optional: override the SQLite location / use another DB
# DATABASE_URL=sqlite:///./onpreo_poc.db
